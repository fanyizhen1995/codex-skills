---
source_id: sglang-github-closed-issues-prs
title: Lightweight extract allocation logic from mem_cache/common.py to more clearly
  show nearly parallel variants
canonical_url: https://github.com/sgl-project/sglang/pull/29431
captured_at: '2026-07-15T23:40:28.375193+00:00'
content_hash: 4d9934780bede8402fe2f9382a6eada56bda1d0b66689dca6ef8426674332cb6
---
# Lightweight extract allocation logic from mem_cache/common.py to more clearly show nearly parallel variants

URL: https://github.com/sgl-project/sglang/pull/29431
State: closed
Labels: 
Closed at: 2026-07-15T06:49:37Z
Merged at: 2026-07-15T06:49:37Z

Part of the `req_pool_idx` / cache / owned-KV decoupling — a stacked refactor chain.

Lightweight extraction of the allocation entry points (`alloc_for_extend` /
`alloc_for_decode`, `write_cache_indices`, `get_last_loc`, ...) and the alloc-sizing
helpers out of the `mem_cache/common.py` junk drawer into `allocation.py` +
`allocation_sizing.py`, so near-parallel variants sit together. Soft module
boundary, pure relocation + import rewiring, no behavior change.

## Move + postpare

Commit 1 `9992594a` is a pure relocation of the allocation entry points + alloc-sizing
helpers out of `mem_cache/common.py` into `allocation.py` / `allocation_sizing.py`,
verbatim (generator-inferred reproduce proof).

Commit 2 `2a9c42bd` is a small human-reviewed postpare the move cannot derive: use the
already-imported `get_server_args()` in `_alloc_page_size` (identical value to the legacy
`get_global_server_args()` the source used), repoint the `pr_fix_toggle` debug target
string to `allocation_sizing`, and lower the `get_global_server_args` ratchet baseline to 279.

### Verify the certified moves

All four certified relocations in this stack (op40's extract + op42's three) reproduce
byte-for-byte from their base via faithful relocation primitives:

```bash
gh gist clone ec486009606de4216efe8359131da16c /tmp/ch31-proofs
cd <sglang-repo-root>
bash /tmp/ch31-proofs/verify_all.sh     # PASS x4
```

---
Stacked on: `op5`. Aggregated CI sandbox: #28636.













































































































































































































































<!-- pr-states:start -->
---
### CI States

Latest PR Test (Base): <!-- slot:pr-test:start -->:x: [Run #29395337712](https://github.com/sgl-project/sglang/actions/runs/29395337712)<!-- slot:pr-test:end -->
Latest PR Test (Extra): <!-- slot:pr-test-extra:start -->:x: [Run #29395337559](https://github.com/sgl-project/sglang/actions/runs/29395337559)<!-- slot:pr-test-extra:end -->
<!-- pr-states:end -->
