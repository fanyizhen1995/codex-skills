# Ingest Plan

Source path: `raw/crawler/sglang-github-closed-issues-prs/manifest-20260709.json`

## Durable claims

- The 2026-07-09 scheduled batch contains 102 page snapshots: 6 closed issues and 96 closed pull requests, of which 69 have a page-visible merged timestamp and 27 are closed without one.
- The batch has 98 new item identities. Pull requests #2, #9754, #22416, and #27988 overlap previously captured identities and must not be counted as new knowledge solely because the page was captured again.
- Merged pull requests #29417, #30339, #30461, #30557, #30602, #30606, #30615, #30645, and #30653 add bounded evidence about unified-KV/HiCache behavior, SWA state correctness, disaggregated draft-state transfer, ROCm graph capture, shared-memory exhaustion, concurrent server-info calls, speculative-decoding metrics, sparse top-k index validity, and merge-order compatibility.
- Page-visible benchmark and test results remain source-owned evidence. They are not local benchmark baselines, production SLOs, complete postmortems, or a substitute for an API refresh with joined comments and review comments.

## Target pages

- Update `wiki/references/sglang-github-closed-issues-prs.md` with the batch scope, duplicate boundary, and selected operational signals.
- Update `wiki/references/inference-runtime-infrastructure.md` with the selected merged runtime reliability evidence.
- Update `wiki/references/ai-infra-coverage-map.md` and `coverage-map.json` with bounded inference-runtime coverage.
- Update `ingest.md`, rebuild the domain index and backlinks, then run domain and full validation.

## Non-goals

- Do not combine page snapshots into the June API corpus counts.
- Do not describe closed-unmerged pull requests or issue-only reports as shipped fixes.
- Do not create a new SGLang project page or duplicate the existing inference-runtime reference.
- Do not promote source-owned benchmark numbers into a local baseline, ranking, SLO, or general performance guarantee.

## Compact decision

The batch is about 457 KB across 102 small Markdown files. Keep the files readable because the manifest provides identity-level navigation and individual pages remain useful troubleshooting evidence; gzip compaction would add little value at this size.
