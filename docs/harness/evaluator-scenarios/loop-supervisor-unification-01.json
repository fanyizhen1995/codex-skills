{
  "task_id": "loop-supervisor-unification-01",
  "must_simulate": true,
  "user_scenarios": [
    {
      "scenario_id": "LOOP-SUPERVISOR-UNIFICATION-BROWSER-E2E",
      "user_goal": "作为操作者，在统一 Loop Dashboard 中使用七个 Supervisor 页签和七个运行详情页签，并在真实游标分页下稳定查看完整中文记录。",
      "prerequisites": [
        "Evaluator 使用 Task 6 SupervisorStore.open 初始化临时 SQLite，不读取 live/main 运行状态。",
        "Evaluator 为隔离 backend 显式设置自己的 LOOP_DASHBOARD_CURSOR_SECRET。",
        "前端静态加载顺序固定为 pagination.js -> supervisor.js -> app.js，分页大小固定为 20/50/100。",
        "父 run 与子 run 均通过 backend-owned planner-output.json、generator-result.json、evaluator-result.json 和 task-contract.json 生成展示字段。"
      ],
      "entrypoint": "python3 scripts/loop_dashboard_evaluator.py --repo-root . --output-dir .codex/loop-dashboard-eval/loop-supervisor-unification-01 --scenario loop-supervisor-unification-01",
      "steps": [
        "初始化临时 Supervisor SQLite，并分别写入 26 条 actions、reviews、user decisions 和 Skill inventory 行。",
        "创建 artifact-backed 父子运行，并为父 run 写入 26 条 events 和 26 个 logs；日志正文只允许在显式展开后请求。",
        "在 1440x1000 Chromium 中依次检查七个 Supervisor 页签和七个运行详情页签。",
        "延迟第一页任务恢复响应并立即修改过滤器，确认旧响应不能覆盖最新状态。",
        "延迟完整服务列表响应后立即选择运行，确认 Supervisor deactivate 会取消请求且旧 generation 不能覆盖运行视图。",
        "在第一页之后仍存在隐藏服务记录的夹具中读取全部 service/freshness 页，确认隐藏的不健康或过期记录会使全局健康降级。",
        "展开 action-001 的 421 条恢复尝试，连续进入第 21 页后刷新并重新展开，确认 page offset 和非空首游标窗口可恢复。",
        "在任务恢复第一页确认只有已访问页码 1；拦截第一次 Next 返回结构化 capacity_exceeded，确认保留第一页、恢复建议和重试控件，再重试进入第 21-26 条。",
        "使用可见刷新命令和浏览器刷新恢复任务恢复第二页、运行 ID 过滤器和游标历史；插入新动作后第二页内容保持不变。",
        "将 Reviewer 每页大小改为 50，确认与任务恢复的过滤器、第二页和游标历史相互独立并在刷新后恢复。",
        "创建 40 个 run/tab pager 状态，确认 URL 只有一个 dashboard_state token、session 最多保留 24 个 pager 且总长度有界。",
        "直接打开 ?run_id=dashboard-parent-001&run_tab=agents，确认视图和页签同步且只请求选中集合所需数据。",
        "检查完整任务说明、父子 Agent 尝试/结果/产物、验收检查/证据/复验命令、diagnostic 严重性/来源和 artifact 路径/时间。",
        "确认运行详情加载不预取 events/logs，日志列表不含正文，显式展开后才请求 bounded log detail，并验证展开控件 ARIA。",
        "使用 Left/Right/Home/End 验证 Supervisor 和运行详情 tabs 的 roving tabindex、焦点与选中态。",
        "在 390x844 Chromium 中打开服务表格，检查单列布局、内部可滚动 tabs/table 和 document 无横向溢出。",
        "保存 desktop、run-detail-desktop 与 mobile 截图，并检查截图 canvas 不是空白。"
      ],
      "expected_outcomes": [
        "Supervisor 是运行列表外唯一项目级控制角色。",
        "Supervisor 页签严格为概览、服务、任务恢复、Reviewer、决策、Skill 治理、配置。",
        "运行详情页签严格为概览、子任务、Agent 结果、验收、日志、阻塞诊断、产物。",
        "Auditor、orchestrator 和 auto-resume 不作为独立角色出现。",
        "未知数字页不能跳转；下一页记录游标后才暴露对应数字页。",
        "失败 Next 保留已渲染页并显示结构化错误、恢复动作和可用重试；旧请求不能覆盖新过滤状态。",
        "刷新恢复 tab/page/filter/history，Reviewer page-size 50 与任务恢复状态独立，快照中的第二页不受新插入记录影响。",
        "第 21 页刷新后恢复页码 2-21 的有界窗口，首个保留 cursor 可以非空且 offset 保持准确。",
        "URL 只保存一个全局有界 session token，最多保留 24 个 pager，不包含完整签名 cursor 链，直接 run URL 恢复选中视图。",
        "切换到 run 会取消 Supervisor 请求；全局健康仅在完整 service/freshness 覆盖且全部通过时显示正常。",
        "Reviewer cadence 和未过滤 finding 计数不伪装成已知配置或开放数量；schema 错误结构化显示 status/code/message。",
        "run phase、Agent status、ActionType、decision 和 service 状态均按 authoritative contract 显示中文标签和严重性。",
        "子任务状态过滤器使用 phase/health 值，恢复尝试通过 exact Task 7 envelope 和内部 Next 控件完整可达。",
        "运行详情全部语义来自真实父子 artifact，完整正文、验收证据、诊断来源和产物 provenance 可读。",
        "所有不可用数据明确显示暂无数据、未启用或不可用，不显示假成功。",
        "ARIA tabs 支持键盘焦点移动，日志和恢复日志按钮暴露 aria-expanded/aria-controls。",
        "桌面、运行详情和移动截图中文字完整；移动 table/tabs 内部滚动且无页面级横向溢出或重叠。"
      ],
      "failure_signals": [
        "Evaluator 依赖 ambient cursor secret 或连接 live service。",
        "loadSelectedRun 预取全部 events/logs，或日志列表假定 content 字段存在。",
        "旧请求覆盖新过滤器，失败 Next 丢失上一页，或结构化恢复动作不可见。",
        "切换运行后延迟 Supervisor 响应仍修改 run/global UI，或隐藏的不健康服务未影响全局健康。",
        "未访问的数字页可点击，第 21 页刷新丢失 offset window，page-size 状态串页，或新记录改变已打开第二页。",
        "URL 为每个 pager 累积参数、session pager 数量无界、暴露完整 cursor 链、直接运行 URL 仍显示 Supervisor，或非选中 run 集合被预取。",
        "恢复尝试只显示前 20 条、子任务使用 acceptance 状态过滤值、contract enum 显示原始英文或 schema diagnostics 显示对象字符串。",
        "Agent/acceptance/diagnostic 数据仅伪造在 run.json 而不是来自真实 artifact。",
        "运行 section 与 Agent/acceptance 卡片形成嵌套边框容器。",
        "浏览器可见独立 Auditor、orchestrator 或 auto-resume 角色。",
        "desktop/run-detail/mobile 截图空白、文本截断、控件重叠或 document-level overflow。"
      ],
      "cleanup": [
        "停止隔离 dashboard 子进程。",
        "移除临时 fixture 目录；仅 evaluator artifact 保留在指定 output-dir。"
      ],
      "automation_hint": "playwright"
    },
    {
      "scenario_id": "LOOP-SUPERVISOR-ISOLATED-RUNTIME-E2E",
      "user_goal": "作为运行维护者，在不读取 live/main 状态的临时 Git 项目中证明统一 Supervisor、Worker、恢复和 Reviewer 的端到端语义。",
      "prerequisites": [
        "Evaluator 创建临时 Git repository、Supervisor SQLite 和隔离 Dashboard cursor secret。",
        "Supervisor、Worker 和 Dashboard 均从当前 feature worktree 代码启动，不复用 live service。",
        "Agent 与 Reviewer 使用 deterministic fixture driver；调度、租约、恢复、结果验证和 Reviewer application 使用真实 runtime path。"
      ],
      "entrypoint": "python3 scripts/loop_supervisor_e2e_evaluator.py --repo-root . --output-dir .codex/loop-supervisor-e2e/loop-supervisor-unification-01",
      "steps": [
        "启动 Supervisor watch 三个 tick，检查同一 run revision 只产生一个幂等 action，并追加第三次 reconcile counterexample rerun。",
        "持有 run lock 后启动 Worker，等待其获得 lease，再以 SIGKILL 模拟 Worker crash；加速 lease/heartbeat 过期后启动 replacement Worker。",
        "检查 replacement Worker 只生成一条 action attempt，完成后再次尝试 reclaim，确认 completed action 不会重复领取。",
        "为 parent-22 写入 Generator attempts 3/4、验证 manifest、gap proof、dirty path 和 artifact hash，注入三次 retryable model-capacity failure。",
        "确认 Supervisor 只排队 recover_generator_result，Worker 重建 generator-result envelope，随后由独立 Evaluator action 生成 passing evaluator-result。",
        "为 parent-22 打开 run-scoped user decision，同时排队 independent continuation，确认后者仍可领取且没有 global stop。",
        "写入两个 semantic parent completion，确认 cadence 只触发一次真实 project-global Reviewer fixture invocation，candidate 经过 schema/evidence validation 后入库。",
        "再写入两个 semantic parent completion 并注入 Reviewer timeout，确认记录 review_degraded、deterministic safety gate 通过且 fail-open。",
        "在隔离 Dashboard 上运行 browser tabs/pagination acceptance，并独立检查 DB rows、leases、run files、action provenance、browser content 和 Git commits。",
        "检查 canonical Supervisor CLI 与浏览器均不暴露 Auditor、orchestrator 或 auto-resume 公共角色。"
      ],
      "expected_outcomes": [
        "重复 reconcile 只保留一条 action。",
        "Worker crash lease 恰好回收一次，attempt provenance 指向 replacement Worker。",
        "parent-22 recovery envelope 保留 attempts 3/4 hash provenance，并在 recover_generator_result 后运行独立 Evaluator。",
        "run-scoped failure 不阻塞 independent continuation，也不创建 global stop。",
        "每两个 semantic parent 只产生一条 project-global Reviewer record；Reviewer timeout fail-open 并记录 review_degraded。",
        "数据库和浏览器均包含超过一页记录，七个 Supervisor tabs 与 cursor pagination 通过真实 API/Playwright 检查。",
        "Auditor、orchestrator 和 auto-resume 不作为公共 runtime role 出现。",
        "每个 suspicious observation 都有 counterexample rerun 结果，不能只凭 fixture rendering 报告 runtime pass。"
      ],
      "failure_signals": [
        "Evaluator 读取 live/main Supervisor DB、服务或 run state。",
        "Worker crash 后 action 永久 leased、被回收多次或失去 attempt provenance。",
        "Supervisor fresh-rerun Generator，未选择 recover_generator_result，或 recovered envelope 未运行 Evaluator。",
        "run-scoped decision 阻塞其他 run，或 Reviewer timeout 产生 global stop。",
        "两个 parent 产生零条或多条 Reviewer invocation，Reviewer candidate 未经过 evidence/schema validation。",
        "runtime scenario 仅复用 Dashboard fixture 行，未检查 DB、lease、run artifact、action provenance 和 Git commit。",
        "浏览器或 CLI 仍显示 Auditor、orchestrator 或 auto-resume 公共角色。"
      ],
      "cleanup": [
        "终止 isolated Supervisor、Worker 和 Dashboard 子进程。",
        "删除临时 runtime/browser Git project；保留 output-dir 下 result.json、process evidence 和 screenshots。"
      ],
      "automation_hint": "python-subprocess-sqlite-playwright"
    }
  ]
}
